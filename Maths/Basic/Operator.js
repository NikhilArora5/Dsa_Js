
/**
 * POST AS DHABA
 * Post increment
 * First we use the value and then second time we use the value is incrmented
 * b=a++ we have use the value of a and assigned ot b then then a get incremeted
 */
let a=10

let b=a++

console.log({a,b})

/**
 * PRE AS DOMINOS
 * Pre increment
 * First we incrmeent then use the value 
 * b=++a we have incremented a first then uses the value of a and assigned to b
 */
let a2=10

let b2=++a2

console.log({a2,b2})


/**EXAMPLE 1
 * let i=11
 * i=i++ + ++i
 *  11  + ++(12)
 *  11+ 13 =24
 * 
 */


/**EXAMPLE 2
 * C
 * i
 * 
 * /















